# Minimal Liverton storefront Vercel contract
# Add required names in both Preview and Production. Enter values only in Vercel.

# Shared Liverton/Manus backend and auth — copy these exact values
# from the existing Liverton project; Firebase does not provide them.
DATABASE_URL=
JWT_SECRET=
VITE_APP_ID=
OAUTH_SERVER_URL=
VITE_OAUTH_PORTAL_URL=
OWNER_OPEN_ID=

# Shopify Storefront (one token name only)
SHOPIFY_STORE_DOMAIN=
SHOPIFY_STOREFRONT_API_ACCESS_TOKEN=

# Cloudinary server contract is only needed by Admin-Store
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=
# CLOUDINARY_UPLOAD_PRESET=liverton_admin_media

# Firebase Console → Project settings → Your apps → Web app configuration
# Put apiKey, authDomain, databaseURL (if enabled), projectId,
# storageBucket, messagingSenderId, and appId into this one JSON value.
VITE_FIREBASE_CONFIG_JSON={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}

# Optional only when the related storefront feature is enabled
# BUILT_IN_FORGE_API_URL=
# BUILT_IN_FORGE_API_KEY=
# SHOPIFY_ADMIN_ACCESS_TOKEN=
# SHOPIFY_CLIENT_ID=
# SHOPIFY_API_SECRET=

# Banner media and copy are managed in Admin-Store and persisted in the
# shared backend. Do not add banner URL/public-ID variables. Never commit
# real values or use VITE_ for server secrets.
