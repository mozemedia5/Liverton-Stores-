# Firebase + Shopify Liverton storefront Vercel contract
# Add these names in both Preview and Production. Enter values only in Vercel.

# Firebase Console → Project settings → Your apps → Web app configuration
# Put apiKey, authDomain, databaseURL (if enabled), projectId,
# storageBucket, messagingSenderId, and appId into this one JSON value.
VITE_FIREBASE_CONFIG_JSON=

# Firebase Console → Project settings → Service accounts → Generate new private key
# Paste the complete service-account JSON as a server-only Vercel value.
FIREBASE_SERVICE_ACCOUNT_JSON=

# Shopify Storefront API
SHOPIFY_STORE_DOMAIN=
SHOPIFY_STOREFRONT_API_ACCESS_TOKEN=

# Hanna AI — server-only Gemini API access
GEMINI_API_KEY=
# Optional model override; defaults to gemini-3.6-flash
HANNA_GEMINI_MODEL=

# Shopify webhooks and Admin API (server-only)
# Use the Shopify app client secret for HMAC verification.
SHOPIFY_WEBHOOK_SECRET=
# Admin API token with read_products and read_inventory scopes.
SHOPIFY_ADMIN_ACCESS_TOKEN=
# Public HTTPS endpoint, for example https://your-domain.com/api/shopify/webhooks
SHOPIFY_WEBHOOK_CALLBACK_URL=

# Cloudinary signing variables belong in Admin-Store only.
# Banner media and copy are managed in Admin-Store; the storefront reads published media references from Firebase.

# Do not add DATABASE_URL, JWT_SECRET, Manus OAuth variables, Manus Forge variables,
# banner URL variables, or Firebase individual-field variables. Never prefix server
# secrets with VITE_. Rotate any previously exposed credentials before use.
